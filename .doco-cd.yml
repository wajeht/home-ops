# Explicit deployment configs for each service

name: traefik
working_dir: infrastructure/traefik
compose_files:
  - docker-compose.yml

---

name: doco-cd
working_dir: infrastructure/doco-cd
compose_files:
  - docker-compose.yml

---

name: prometheus
working_dir: infrastructure/prometheus
compose_files:
  - docker-compose.yml

---

name: homepage
working_dir: apps/homepage
compose_files:
  - docker-compose.yml

---

name: whoami
working_dir: apps/whoami
compose_files:
  - docker-compose.yml
