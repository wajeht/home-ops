x-docker-cd:
  rolling_update: false

services:
  beszel:
    container_name: beszel
    image: henrygd/beszel:0.18@sha256:4c51486968efa0b0a702c1b0967966a2e06fb250b7418f3072d2488faea27c51
    volumes:
      - /home/jaw/data/beszel:/beszel_data
    networks:
      - traefik
      - beszel-internal
    healthcheck:
      test: ["CMD", "/beszel", "health"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 256M
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.beszel.rule=Host(`beszel.jaw.dev`)"
      - "traefik.http.routers.beszel.entrypoints=websecure"
      - "traefik.http.routers.beszel.middlewares=oauth2-admin@file"
      - "traefik.http.services.beszel.loadbalancer.server.port=8090"

  beszel-agent:
    container_name: beszel-agent
    image: henrygd/beszel-agent:0.19@sha256:00c88600e7d120128f623b2deb5257603d464e841fd68f88cc791dcc075f9e46
    env_file:
      - .env
    environment:
      - PORT=45876
      - FILESYSTEM=/dev/nvme0n1p2
      - SENSORS=coretemp_package_id_0,nvme_composite
      - SMART_INTERVAL=1h
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /:/extra-filesystems/host:ro
    devices:
      - /dev/nvme0:/dev/nvme0
    networks:
      - beszel-internal
    healthcheck:
      test: ["CMD", "/agent", "health"]
      interval: 120s
      timeout: 5s
      retries: 3
      start_period: 15s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - SYS_RAWIO
      - SYS_ADMIN
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M

networks:
  traefik:
    external: true
  beszel-internal:
    internal: true
