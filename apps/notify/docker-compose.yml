x-docker-cd:
  rolling_update: true

services:
  notify:
    image: ghcr.io/wajeht/notify:eeb8839@sha256:aec5a0595bcc8b51c732fb062be0472255a7fe65c3d2378f364def7f90c5f3a4
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/notify:/usr/src/app/dist/src/db/sqlite
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.notify.rule=Host(`notify.jaw.dev`)"
      - "traefik.http.routers.notify.entrypoints=websecure"
      - "traefik.http.routers.notify.middlewares=rate-limit-global@file"
      - "traefik.http.services.notify.loadbalancer.server.port=80"

networks:
  traefik:
    external: true
