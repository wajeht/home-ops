services:
  hindsight:
    container_name: hindsight
    image: ghcr.io/vectorize-io/hindsight:0.5.3@sha256:d68b1b0db2001ad5455d286b40ed1314ee2e09e2abb1d661a5d45599e5c19a7f
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
      - HINDSIGHT_API_RETAIN_BATCH_ENABLED=false
    volumes:
      - /home/jaw/data/hindsight/pg:/home/hindsight/.pg0
    networks:
      - traefik
    ports:
      - "8888:8888"
      - "9999:9999"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    shm_size: 256m
    stop_grace_period: 30s
    oom_score_adj: -300
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "3.0"
          memory: 3G
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hindsight.rule=Host(`hindsight.jaw.dev`)"
      - "traefik.http.routers.hindsight.entrypoints=websecure"
      - "traefik.http.routers.hindsight.middlewares=google-auth@file"
      - "traefik.http.services.hindsight.loadbalancer.server.port=9999"

  hindsight-borgmatic:
    container_name: hindsight-borgmatic
    image: ghcr.io/borgmatic-collective/borgmatic:2.1.5@sha256:f6b0a640d72bf7bb628479246e7c919404917cc58ddc6cdfb8d2d40d786d3a26
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/hindsight:/source/data
      - /home/jaw/backup/hindsight:/repository
      - /home/jaw/data/hindsight/borgmatic:/borgmatic/state
      - ./borgmatic-config.yml:/etc/borgmatic/config.yaml:ro
      - ./borgmatic-crontab.txt:/etc/borgmatic.d/crontab.txt:ro
    networks:
      - backup
    healthcheck:
      test: ["CMD-SHELL", "borgmatic --version"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - DAC_READ_SEARCH
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  traefik:
    external: true
  backup:
    external: true
