services:
  hindsight:
    container_name: hindsight
    image: ghcr.io/vectorize-io/hindsight:0.5.1@sha256:2fa86f472707224307d540bd4ed4c3109ecac42a7be765287256f314cdd6b844
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
      - HINDSIGHT_API_RETAIN_BATCH_ENABLED=true
    volumes:
      - /home/jaw/data/hindsight/pg:/home/hindsight/.pg0
    networks:
      - traefik
    ports:
      - "8888:8888"
      - "9999:9999"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    shm_size: 256m
    stop_grace_period: 30s
    oom_score_adj: -300
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "3.0"
          memory: 3G
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.hindsight.rule=Host(`hindsight.jaw.dev`)"
      - "traefik.http.routers.hindsight.entrypoints=websecure"
      - "traefik.http.routers.hindsight.middlewares=google-auth@file"
      - "traefik.http.services.hindsight.loadbalancer.server.port=9999"

  hindsight-borgmatic:
    container_name: hindsight-borgmatic
    image: ghcr.io/borgmatic-collective/borgmatic:2.1.4@sha256:201001bead2ad04bd46567def5cc63fe277749a1ee7f2e544eb0a415b64f6706
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/hindsight:/source/data
      - /home/jaw/backup/hindsight:/repository
      - /home/jaw/data/hindsight/borgmatic:/borgmatic/state
      - ./borgmatic-config.yml:/etc/borgmatic/config.yaml:ro
      - ./borgmatic-crontab.txt:/etc/borgmatic.d/crontab.txt:ro
    networks:
      - backup
    healthcheck:
      test: ["CMD-SHELL", "borgmatic --version"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - DAC_READ_SEARCH
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  traefik:
    external: true
  backup:
    external: true
