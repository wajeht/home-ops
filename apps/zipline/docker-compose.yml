services:
  zipline:
    container_name: zipline
    image: ghcr.io/diced/zipline:v4
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://zipline:${POSTGRES_PASSWORD}@zipline-db:5432/zipline
      - TZ=America/Chicago
    depends_on:
      zipline-db:
        condition: service_healthy
    volumes:
      - /home/jaw/data/zipline/uploads:/zipline/uploads
      - /home/jaw/data/zipline/public:/zipline/public
      - /home/jaw/data/zipline/themes:/zipline/themes
    networks:
      - traefik
      - zipline-internal
    healthcheck:
      test: ["CMD", "wget", "-O", "/dev/null", "-q", "http://0.0.0.0:3000"]
      interval: 30s
      timeout: 10s
      retries: 5
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zipline.rule=Host(`img.jaw.dev`)"
      - "traefik.http.routers.zipline.entrypoints=websecure"
      - "traefik.http.routers.zipline.middlewares=google-auth@file"
      - "traefik.http.services.zipline.loadbalancer.server.port=3000"

  zipline-db:
    container_name: zipline-db
    image: postgres:17-alpine@sha256:6f30057d31f5861b66f3545d4821f987aacf1dd920765f0acadea0c58ff975b1
    env_file:
      - .env
    environment:
      - POSTGRES_USER=zipline
      - POSTGRES_DB=zipline
      - PGDATA=/var/lib/postgresql/data
    volumes:
      - /home/jaw/data/zipline/db:/var/lib/postgresql/data
    networks:
      - zipline-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zipline"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M

  zipline-db-dump:
    image: postgres:17-alpine@sha256:6f30057d31f5861b66f3545d4821f987aacf1dd920765f0acadea0c58ff975b1
    environment:
      - PGPASSWORD=${POSTGRES_PASSWORD}
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        echo "pg_dump sidecar for zipline"
        while true; do
          pg_dump -h zipline-db -U zipline -Fc zipline > /dumps/zipline.dump.tmp && mv /dumps/zipline.dump.tmp /dumps/zipline.dump && echo "dump ok $$(date)"
          sleep 86400
        done
    volumes:
      - /home/jaw/data/zipline/dumps:/dumps
    networks:
      - zipline-internal
    depends_on:
      zipline-db:
        condition: service_healthy
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 256M

  zipline-borgmatic:
    image: ghcr.io/borgmatic-collective/borgmatic:2.1.2@sha256:961533d6135fd67736e9fee0f7cebc4926b57840d4a210be0a0cf2de6b004996
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/zipline/dumps:/source/dumps:ro
      - /home/jaw/data/zipline/borg:/repository
      - /home/jaw/data/zipline/borgmatic:/borgmatic/state
      - ./borgmatic-config.yml:/etc/borgmatic/config.yaml:ro
      - ./borgmatic-crontab.txt:/etc/borgmatic.d/crontab.txt:ro
    networks:
      - traefik
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - DAC_READ_SEARCH
      - FOWNER
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M

networks:
  traefik:
    external: true
  zipline-internal:
