services:
  power-badge:
    image: ghcr.io/wajeht/power-badge:1488853
    container_name: power-badge
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.power-badge.rule=Host(`power-badge.jaw.dev`)"
      - "traefik.http.routers.power-badge.entrypoints=websecure"
      - "traefik.http.routers.power-badge.middlewares=rate-limit-global@file"
      - "traefik.http.services.power-badge.loadbalancer.server.port=80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 32M

networks:
  traefik:
    external: true
