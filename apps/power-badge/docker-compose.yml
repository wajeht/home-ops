x-docker-cd:
  rolling_update: false

services:
  power-badge:
    image: ghcr.io/wajeht/power-badge:eb816fc@sha256:4368f69eaa61f5eef7bd129db6fabed7ae5c2ef9e7e55db8600f436ef09a002a
    container_name: power-badge
    restart: unless-stopped
    init: true
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.power-badge.rule=Host(`power-badge.jaw.dev`)"
      - "traefik.http.routers.power-badge.entrypoints=websecure"
      - "traefik.http.routers.power-badge.middlewares=rate-limit-global@file"
      - "traefik.http.services.power-badge.loadbalancer.server.port=80"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 32M

networks:
  traefik:
    external: true
