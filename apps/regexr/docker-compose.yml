x-docker-cd:
  rolling_update: false

services:
  regexr:
    image: ghcr.io/wajeht/regexr:v1.0.1@sha256:0cfb8f4806ac567f36f3c20d36c007f2d9b251f4124071dae28f8dd25325b40e
    networks:
      - traefik
    healthcheck:
      test:
        [
          "CMD",
          "php",
          "-r",
          'exit(@file_get_contents("http://127.0.0.1:8080/healthz") === "ok\n" ? 0 : 1);',
        ]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.regexr.rule=Host(`regex.jaw.dev`)"
      - "traefik.http.routers.regexr.entrypoints=websecure"
      - "traefik.http.routers.regexr.middlewares=rate-limit-global@file"
      - "traefik.http.services.regexr.loadbalancer.server.port=8080"

networks:
  traefik:
    external: true
