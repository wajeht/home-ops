services:
  stirling-pdf:
    image: frooodle/s-pdf:2.4.4@sha256:c08373b5980696ae188a67f68f66e40742f2475280f546e3dbb39953e5e1e883
    environment:
      - TZ=America/Chicago
      - DOCKER_ENABLE_SECURITY=false
    volumes:
      - /home/jaw/data/stirling-pdf:/usr/share/tessdata
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/info/status"]
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
        order: start-first
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.stirling-pdf.rule=Host(`pdf.jaw.dev`)"
        - "traefik.http.routers.stirling-pdf.entrypoints=websecure"
        - "traefik.http.routers.stirling-pdf.middlewares=authelia@file"
        - "traefik.http.services.stirling-pdf.loadbalancer.server.port=8080"

networks:
  traefik:
    external: true
