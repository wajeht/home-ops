sqlite_databases:
  - name: favicon
    path: /source/data/db.sqlite

repositories:
  - path: /repository
    label: favicon

archive_name_format: "favicon-{now:%Y-%m-%d-%H%M%S}"
compression: zstd,3

keep_daily: 30
keep_weekly: 12
keep_monthly: 12

checks:
  - name: repository
    frequency: 1 week
  - name: archives
    frequency: 1 week

check_last: 3

ntfy:
  topic: borgmatic
  server: http://ntfy:80
  finish:
    title: "favicon db backup complete"
    message: "favicon database backup finished"
    priority: min
    tags: white_check_mark
  fail:
    title: "favicon db backup FAILED"
    message: "favicon database backup failed"
    priority: max
    tags: skull
  states:
    - finish
    - fail
