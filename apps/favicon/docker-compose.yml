x-docker-cd:
  rolling_update: true

services:
  favicon:
    image: ghcr.io/wajeht/favicon:e4edf94@sha256:d1e7e9d8ce79d6522863c471223777c5009221856fc7fc89e833fddfadc44a56
    environment:
      TZ: America/Chicago
    volumes:
      - /home/jaw/data/favicon:/app/data
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.favicon.rule=Host(`favicon.jaw.dev`)"
      - "traefik.http.routers.favicon.entrypoints=websecure"
      - "traefik.http.routers.favicon.middlewares=rate-limit-global@file"
      - "traefik.http.services.favicon.loadbalancer.server.port=80"

networks:
  traefik:
    external: true
