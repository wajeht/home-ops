x-docker-cd:
  rolling_update: true

services:
  ufc:
    image: ghcr.io/wajeht/ufc:9e37045@sha256:6ca4694594a339f131bb2b4c68e473dfd16b26de2576633b479dd3693c8f485e
    environment:
      TZ: America/Chicago
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ufc.rule=Host(`ufc.jaw.dev`)"
      - "traefik.http.routers.ufc.entrypoints=websecure"
      - "traefik.http.routers.ufc.middlewares=rate-limit-global@file"
      - "traefik.http.services.ufc.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
