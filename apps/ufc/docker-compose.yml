x-docker-cd:
  rolling_update: true

services:
  ufc:
    image: ghcr.io/wajeht/ufc:d842cac@sha256:f3c480759e4be0c09fd6bcfcc1c0c6f07065a49941935cc2bc8a7790c2821a7d
    environment:
      TZ: America/Chicago
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ufc.rule=Host(`ufc.jaw.dev`)"
      - "traefik.http.routers.ufc.entrypoints=websecure"
      - "traefik.http.routers.ufc.middlewares=rate-limit-global@file"
      - "traefik.http.services.ufc.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
