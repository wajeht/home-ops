services:
  www:
    image: ghcr.io/wajeht/jaw.dev:3c3dd64@sha256:b1b38c949d9821f482ce2d3d89d5a2515fea2361b4e6f8d971eeec99a81fb5fb
    environment:
      - TZ=America/Chicago
      - HTTP_PORT=80
    networks:
      - traefik
    healthcheck:
      test:
        - CMD
        - curl
        - "-f"
        - http://localhost:80
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    labels:
      - traefik.enable=true
      - traefik.http.routers.jaw-dev-pr-38.rule=Host(`pr-38-jaw-dev.jaw.dev`) || Host(`pr-38-jaw-dev.jaw.dev`)
      - traefik.http.routers.jaw-dev-pr-38.entrypoints=websecure
      - traefik.http.routers.jaw-dev-pr-38.middlewares=oauth2-admin@file
      - traefik.http.services.jaw-dev-pr-38.loadbalancer.server.port=80
networks:
  traefik:
    external: true
