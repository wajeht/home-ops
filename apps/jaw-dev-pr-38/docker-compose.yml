services:
  www:
    image: ghcr.io/wajeht/jaw.dev:18ff9ce@sha256:f85519a31c931b5e5f60986c49c6efe83ff0e9179191030d04558b8a9c3a08c8
    environment:
      - TZ=America/Chicago
      - HTTP_PORT=80
    networks:
      - traefik
    healthcheck:
      test:
        - CMD
        - curl
        - "-f"
        - http://localhost:80
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    labels:
      - traefik.enable=true
      - traefik.http.routers.jaw-dev-pr-38.rule=Host(`pr-38-jaw-dev.jaw.dev`) || Host(`pr-38-jaw-dev.jaw.dev`)
      - traefik.http.routers.jaw-dev-pr-38.entrypoints=websecure
      - traefik.http.routers.jaw-dev-pr-38.middlewares=oauth2-admin@file
      - traefik.http.services.jaw-dev-pr-38.loadbalancer.server.port=80
networks:
  traefik:
    external: true
