services:
  www:
    image: ghcr.io/wajeht/jaw.dev:8510037@sha256:dbb4ce599a2ba9d52aa9ab8f06c94988c97ae4894a0999ebde7f8b59fa4238ab
    environment:
      - TZ=America/Chicago
      - HTTP_PORT=80
    networks:
      - traefik
    healthcheck:
      test:
        - CMD
        - curl
        - "-f"
        - http://localhost:80
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
    labels:
      - traefik.enable=true
      - traefik.http.routers.jaw-dev-pr-38.rule=Host(`pr-38-jaw-dev.jaw.dev`) || Host(`pr-38-jaw-dev.jaw.dev`)
      - traefik.http.routers.jaw-dev-pr-38.entrypoints=websecure
      - traefik.http.routers.jaw-dev-pr-38.middlewares=oauth2-admin@file
      - traefik.http.services.jaw-dev-pr-38.loadbalancer.server.port=80
networks:
  traefik:
    external: true
