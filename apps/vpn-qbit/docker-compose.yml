services:
  gluetun:
    image: qmcgaw/gluetun:v3.41@sha256:1a5bf4b4820a879cdf8d93d7ef0d2d963af56670c9ebff8981860b6804ebc8ab
    container_name: gluetun
    cap_drop:
      - ALL
    cap_add:
      - NET_ADMIN
      - NET_RAW
      - DAC_OVERRIDE
      - CHOWN
      - FOWNER
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
      - SERVER_COUNTRIES=United States
      - SERVER_CITIES=Dallas Texas
      - WIREGUARD_MTU=1400
      - FIREWALL_VPN_INPUT_PORTS=45839
      - BLOCK_MALICIOUS=off
      - HTTPPROXY=on
      - HTTPPROXY_LISTENING_ADDRESS=:8888
    tmpfs:
      - /tmp
    volumes:
      - /home/jaw/data/gluetun:/gluetun
    networks:
      - traefik
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vpn-qbit.rule=Host(`qbit.jaw.dev`)"
      - "traefik.http.routers.vpn-qbit.entrypoints=websecure"
      - "traefik.http.routers.vpn-qbit.middlewares=oauth2-admin-no-ratelimit@file"
      - "traefik.http.services.vpn-qbit.loadbalancer.server.port=8085"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost:9999 || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 5
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M

  qbittorrent:
    image: linuxserver/qbittorrent:5.2.1@sha256:715d2bfbcf1cd3d734cbbd4fbd599eb7ea0642eaa079a372dd0d343f59516700
    container_name: qbittorrent
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/Chicago
      - WEBUI_PORT=8085
    volumes:
      - /home/jaw/data/qbittorrent:/config
      - /home/jaw/plex:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    restart: unless-stopped
    # lint-ignore: init  # LinuxServer.io qbittorrent uses s6-overlay, must be PID 1
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 1G

networks:
  traefik:
    external: true
