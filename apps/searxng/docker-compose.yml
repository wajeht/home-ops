services:
  searxng:
    container_name: searxng
    image: searxng/searxng@sha256:9206e4c2de22ea36274a67eb961afd90f51d2a9df2f8242b00c94e6c6692a946
    env_file:
      - .env
    environment:
      - SEARXNG_BASE_URL=https://search.jaw.dev
    volumes:
      - /home/jaw/data/searxng:/etc/searxng
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.searxng.rule=Host(`search.jaw.dev`)"
      - "traefik.http.routers.searxng.entrypoints=websecure"
      - "traefik.http.routers.searxng.middlewares=google-auth@file"
      - "traefik.http.services.searxng.loadbalancer.server.port=8080"
networks:
  traefik:
    external: true
