x-docker-cd:
  rolling_update: false

services:
  linx:
    container_name: linx
    image: andreimarcu/linx-server:version-2.3.8@sha256:d969c23e6c324a978f47b2a321384be5781032f660045a46f7a06746f0a4cb06
    command: -config /config/linx.conf
    env_file:
      - .env
    volumes:
      - ./config:/config
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:8080"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - SETGID
      - SETUID
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      # Public file serving — shared links (reads) open to everyone, rate-limited
      - "traefik.http.routers.linx.rule=Host(`linx.jaw.dev`)"
      - "traefik.http.routers.linx.entrypoints=websecure"
      - "traefik.http.routers.linx.middlewares=rate-limit-global@file"
      # Upload UI + endpoints — gated by oauth2-admin (Google SSO, browser)
      - "traefik.http.routers.linx-admin.rule=Host(`linx.jaw.dev`) && (Path(`/`) || PathPrefix(`/upload`) || PathPrefix(`/paste`))"
      - "traefik.http.routers.linx-admin.entrypoints=websecure"
      - "traefik.http.routers.linx-admin.middlewares=oauth2-admin@file"
      - "traefik.http.routers.linx-admin.priority=100"
      - "traefik.http.services.linx.loadbalancer.server.port=8080"
networks:
  traefik:
    external: true
