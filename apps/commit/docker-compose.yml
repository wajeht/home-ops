services:
  commit:
    image: ghcr.io/wajeht/commit:4f2ace4@sha256:edef19e69c968ce5f70eecbd40375227ad7c16b622c86dd368f268a4fb1d4a58
    networks:
      - traefik
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.commit.rule=Host(`commit.jaw.dev`)"
      - "traefik.http.routers.commit.entrypoints=websecure"
      - "traefik.http.routers.commit.middlewares=rate-limit-global@file"
      - "traefik.http.services.commit.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
