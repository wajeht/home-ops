x-docker-cd:
  rolling_update: true

services:
  commit:
    image: ghcr.io/wajeht/commit:6c82ba0@sha256:ab9ef2fdc6e15c3f1bbe33f08a35da8132b18ac07afe644d8b7dae129a431631
    networks:
      - traefik
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.commit.rule=Host(`commit.jaw.dev`)"
      - "traefik.http.routers.commit.entrypoints=websecure"
      - "traefik.http.routers.commit.middlewares=rate-limit-global@file"
      - "traefik.http.services.commit.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
