x-docker-cd:
  rolling_update: true

services:
  commit:
    image: ghcr.io/wajeht/commit:2cdbe0c@sha256:c4ca7f19732763c2385b21fe39e661bc80756a29322761ec2708cac94a9426f7
    networks:
      - traefik
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.commit.rule=Host(`commit.jaw.dev`)"
      - "traefik.http.routers.commit.entrypoints=websecure"
      - "traefik.http.routers.commit.middlewares=rate-limit-global@file"
      - "traefik.http.services.commit.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
