services:
  # lint-ignore: backup  # stateless — polls GitHub notifications, no persistent data
  clear-renovate-notifications:
    image: ghcr.io/wajeht/clear-renovate-notifications:f6307dc@sha256:60e8b070919d748267763490377288d56e9e23abbb052e75c1f39bcdabf07c51
    container_name: clear-renovate-notifications
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
      - DRY_RUN=false
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
networks:
  traefik:
    external: true
