services:
  # lint-ignore: backup  # stateless — polls GitHub notifications, no persistent data
  clear-renovate-notifications:
    image: ghcr.io/wajeht/clear-renovate-notifications:fb8ecc2@sha256:5018d6da5554eada40932b1d448e74ad52b5649aa2b839dea9bfaaef19774185
    container_name: clear-renovate-notifications
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
      - DRY_RUN=false
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
networks:
  traefik:
    external: true
