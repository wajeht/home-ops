x-docker-cd:
  rolling_update: true

services:
  mm2us:
    image: ghcr.io/wajeht/mm2us.com:6cb083b@sha256:71ed05a7a6b2d7aca5c933dd58156ec7ec624014239805e138f7cb1042504496
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/mm2us:/usr/src/app/dist/src/db/sqlite
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mm2us.rule=Host(`mm2us.com`) || Host(`www.mm2us.com`)"
      - "traefik.http.routers.mm2us.entrypoints=websecure"
      - "traefik.http.routers.mm2us.middlewares=rate-limit-global@file"
      - "traefik.http.services.mm2us.loadbalancer.server.port=80"

networks:
  traefik:
    external: true
