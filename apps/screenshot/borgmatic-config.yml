source_directories:
  - /source/data

exclude_patterns:
  - "*/borgmatic"
  - "*/db.sqlite*"

sqlite_databases:
  - name: screenshot
    path: /source/data/db.sqlite

repositories:
  - path: /repository
    label: screenshot

archive_name_format: "screenshot-{now:%Y-%m-%d-%H%M%S}"
compression: zstd,3

keep_daily: 7
keep_weekly: 4
keep_monthly: 6

checks:
  - name: repository
    frequency: 1 week
  - name: archives
    frequency: 1 week

check_last: 3

ntfy:
  topic: borgmatic
  server: http://ntfy:80
  finish:
    title: "screenshot backup complete"
    message: "screenshot backup finished"
    priority: min
    tags: white_check_mark
  fail:
    title: "screenshot backup FAILED"
    message: "screenshot backup failed"
    priority: max
    tags: skull
  states:
    - finish
    - fail
