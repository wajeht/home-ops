x-docker-cd:
  rolling_update: true

services:
  calendar:
    image: ghcr.io/wajeht/calendar:5ce8cf7@sha256:c7f29a4cd55c3e553ea5ae5320d17ae3db4abee5851c660cd63caea3532218c0
    env_file:
      - .env
    environment:
      - TZ=America/Chicago
    volumes:
      - /home/jaw/data/calendar:/usr/src/app/src/db/sqlite
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.calendar.rule=Host(`calendar.jaw.dev`) || Host(`cal.jaw.dev`)"
      - "traefik.http.routers.calendar.entrypoints=websecure"
      - "traefik.http.routers.calendar.middlewares=rate-limit-global@file"
      - "traefik.http.services.calendar.loadbalancer.server.port=80"

networks:
  traefik:
    external: true
