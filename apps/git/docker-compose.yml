services:
  git:
    image: ghcr.io/wajeht/git:a0dc8fa@sha256:751a7fcc9cae46caa94b4fe6a1a52259e686dcedad9d44fa728705174f463e2c
    environment:
      - TZ=America/Chicago
      - APP_PORT=80
      - APP_ENV=production
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:80/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
    restart: unless-stopped
    init: true
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    read_only: true
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.git.rule=Host(`git.jaw.dev`)"
      - "traefik.http.routers.git.entrypoints=websecure"
      - "traefik.http.routers.git.middlewares=rate-limit-global@file"
      - "traefik.http.services.git.loadbalancer.server.port=80"
networks:
  traefik:
    external: true
