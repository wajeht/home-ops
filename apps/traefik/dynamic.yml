http:
  middlewares:
    # Used by the catch-all router for unknown *.jaw.dev subdomains.
    redirect-to-main:
      redirectRegex:
        regex: ".*"
        replacement: "https://jaw.dev"
        permanent: true

    # Compress normal text responses; skip media formats that are already compressed.
    gzip:
      compress:
        excludedContentTypes:
          - image/png
          - image/jpeg
          - image/gif
          - image/webp
          - video/mp4
          - video/webm
          - video/x-matroska
          - audio/mp4
          - audio/aac
          - application/dash+xml
          - application/vnd.apple.mpegurl

    # Origin lock-down: only Cloudflare edge IPs can reach HTTPS routers.
    cloudflare-only:
      ipAllowList:
        sourceRange:
          # BEGIN managed cloudflare source ranges — scripts/cloudflare.sh
          - 173.245.48.0/20
          - 103.21.244.0/22
          - 103.22.200.0/22
          - 103.31.4.0/22
          - 141.101.64.0/18
          - 108.162.192.0/18
          - 190.93.240.0/20
          - 188.114.96.0/20
          - 197.234.240.0/22
          - 198.41.128.0/17
          - 162.158.0.0/15
          - 104.16.0.0/13
          - 104.24.0.0/14
          - 172.64.0.0/13
          - 131.0.72.0/22
          - 2400:cb00::/32
          - 2606:4700::/32
          - 2803:f800::/32
          - 2405:b500::/32
          - 2405:8100::/32
          - 2a06:98c0::/29
          - 2c0f:f248::/32
          # END managed cloudflare source ranges

    # Browser-facing safety headers shared by HTTPS apps.
    security-headers:
      headers:
        stsSeconds: 31536000
        stsIncludeSubdomains: true
        stsPreload: true
        forceSTSHeader: true
        contentTypeNosniff: true
        customFrameOptionsValue: "SAMEORIGIN"
        referrerPolicy: "same-origin"
        permissionsPolicy: "camera=(), microphone=(), geolocation=()"
        customResponseHeaders:
          Cross-Origin-Opener-Policy: "same-origin"
          Cross-Origin-Resource-Policy: "same-site"

    # General rate limit. Uses Cloudflare's real-client IP header.
    rate-limit-global:
      rateLimit:
        period: 1s
        average: 200
        burst: 400
        sourceCriterion:
          requestHeaderName: CF-Connecting-IP

    # Rate limit for auth-protected routes. Burst is wide enough for
    # SPA bundle loads (e.g. Jellyfin fans out 25+ parallel chunk requests).
    rate-limit-auth:
      rateLimit:
        period: 1s
        average: 50
        burst: 200
        sourceCriterion:
          requestHeaderName: CF-Connecting-IP

    # API-specific rate limit for public endpoints.
    rate-limit-api:
      rateLimit:
        period: 1s
        average: 50
        burst: 100
        sourceCriterion:
          requestHeaderName: CF-Connecting-IP

    # OAuth2 admin auth.
    oauth2-admin-forward:
      forwardAuth:
        address: http://oauth2-admin:4180/
        trustForwardHeader: true
        maxResponseBodySize: 4096
        authResponseHeaders:
          - X-Auth-Request-User
          - X-Auth-Request-Email

    # OAuth2 media auth.
    oauth2-media-forward:
      forwardAuth:
        address: http://oauth2-media:4180/
        trustForwardHeader: true
        maxResponseBodySize: 4096
        authResponseHeaders:
          - X-Auth-Request-User
          - X-Auth-Request-Email

    # Standard admin auth chain: rate limit first, then OAuth2 auth.
    oauth2-admin:
      chain:
        middlewares:
          - rate-limit-auth
          - oauth2-admin-forward

    # Admin auth without rate limiting for routes that already limit elsewhere.
    oauth2-admin-no-ratelimit:
      chain:
        middlewares:
          - oauth2-admin-forward

    # Media auth chain: wider allowlist for Plex, Seerr, and ConvertX.
    oauth2-media:
      chain:
        middlewares:
          - rate-limit-auth
          - oauth2-media-forward

  routers:
    # Low-priority fallback: unknown *.jaw.dev subdomains redirect to jaw.dev.
    catchall:
      rule: "HostRegexp(`[a-z0-9-]+\\.jaw\\.dev`)"
      entrypoints:
        - websecure
      middlewares:
        - redirect-to-main
      service: dummy
      priority: 1
      tls:
        domains:
          - main: jaw.dev
            sans:
              - "*.jaw.dev"

  services:
    # Placeholder service required by redirect-only routers.
    dummy:
      loadBalancer:
        servers:
          - url: "http://127.0.0.1"
