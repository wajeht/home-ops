theme: dark
default_2fa_method: totp

server:
  address: tcp://0.0.0.0:9091/

log:
  level: info

totp:
  issuer: wajeht.com

authentication_backend:
  file:
    path: /config/users.yml
    password:
      algorithm: argon2id

access_control:
  default_policy: deny
  rules:
    - domain: "*.wajeht.com"
      policy: one_factor

session:
  secret: "{{ secret '/run/secrets/authelia_session_secret' | msquote }}"
  name: authelia_session
  same_site: lax
  expiration: 1h
  inactivity: 5m
  remember_me: 1M
  cookies:
    - domain: wajeht.com
      authelia_url: https://auth.wajeht.com

storage:
  encryption_key: "{{ secret '/run/secrets/authelia_storage_encryption_key' | msquote }}"
  local:
    path: /config/db.sqlite3

notifier:
  filesystem:
    filename: /config/notifications.txt

identity_validation:
  reset_password:
    jwt_secret: "{{ secret '/run/secrets/authelia_jwt_secret' | msquote }}"

# OIDC Identity Provider (uncomment to enable Google SSO)
# identity_providers:
#   oidc:
#     hmac_secret: "{{ secret '/run/secrets/authelia_oidc_hmac_secret' | msquote }}"
#     jwks:
#       - key: "{{ secret '/run/secrets/authelia_oidc_jwks_key' | msquote }}"
#     clients:
#       - client_id: google
#         client_name: Google
#         client_secret: "YOUR_GOOGLE_CLIENT_SECRET_HASH"
#         public: false
#         authorization_policy: one_factor
#         redirect_uris:
#           - https://auth.wajeht.com/api/oidc/callback
#         scopes:
#           - openid
#           - profile
#           - email
#         userinfo_signed_response_alg: none
