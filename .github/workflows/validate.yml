name: Validate

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: [self-hosted, linux, default]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 25
      - run: ./scripts/lint.sh
