# Copy to .env and fill in values
# SECURITY: Never commit .env to git

# =============================================================================
# GENERAL
# =============================================================================
TZ=America/Los_Angeles
DOMAIN=home.local

# =============================================================================
# GIT ACCESS (use minimal permissions - read-only repo access only)
# =============================================================================
# GitHub: fine-grained token with Contents + Metadata read-only
# GitLab: read_repository scope, Reporter role
# Gitea: Repository read permission
GITOPS_REPO_URL=https://github.com/youruser/home-ops.git
GIT_ACCESS_TOKEN=ghp_xxxxxxxxxxxx

# =============================================================================
# HTTPS / LET'S ENCRYPT
# =============================================================================
ACME_EMAIL=admin@example.com
# Cloudflare: API token with Zone:DNS:Edit permission
CF_DNS_API_TOKEN=

# =============================================================================
# DOCO-CD SETTINGS
# =============================================================================
# API secret for REST API access (generate: openssl rand -hex 32)
API_SECRET=
# Webhook secret for git provider webhooks (generate: openssl rand -hex 32)
WEBHOOK_SECRET=

# =============================================================================
# NOTIFICATIONS (optional)
# =============================================================================
# Apprise URL for notifications (e.g., discord://, slack://, telegram://)
# See: https://github.com/caronc/apprise/wiki
# APPRISE_NOTIFY_URLS=discord://webhook_id/webhook_token
# APPRISE_NOTIFY_LEVEL=success  # info, success, warning, failure

# =============================================================================
# SECRETS ENCRYPTION (optional)
# =============================================================================
# Generate age key: age-keygen -o secrets/sops_age_key.txt
# Encrypt files: sops encrypt --age <public_key> secrets.env
# SOPS_AGE_KEY_FILE=/run/secrets/sops_age_key

# =============================================================================
# DEPLOYMENT
# =============================================================================
# Minimum 2 for zero-downtime rolling updates
REPLICAS=2
